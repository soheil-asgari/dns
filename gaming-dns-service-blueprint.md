# نقشه راه ساخت سرویس DNS/Proxy گیمینگ — از صفر تا MVP

این سند برای این طراحی شده که مستقیماً به Claude Code بدهید. هر فاز شامل: هدف، پیش‌نیاز، تسک‌های مشخص، و یک "پرامپت پیشنهادی" برای Claude Code است.

---

## فاز ۰ — پیش‌نیازها (قبل از هر خطی کد)

قبل از اینکه Claude Code را صدا بزنید، این‌ها باید آماده باشد (خودتان دستی تهیه می‌کنید، کد نیست):

1. **یک سرور در ایران** (آسیاتک/رسپینا/شاتل) — حداقل 2GB RAM، Ubuntu 22.04
2. **یک سرور خارج** نزدیک به سرورهای گیم (فرانکفورت/آمستردام) — حداقل 2GB RAM
3. **یک دامنه** برای پنل و API (مثلاً `panel.example.com`, `api.example.com`)
4. حساب کاربری در یک **Registrar** برای مدیریت DNS دامنه خودتان
5. **گیت‌هاب ریپو خصوصی** برای نگه‌داری کد (چون شامل کانفیگ حساس می‌شود)

> نکته: چون بخشی از این کار (bypass سرویس‌های تحریم‌شده) خاکستری است، لاگ کاربران را حداقلی نگه دارید و از HTTPS/رمزنگاری سرتاسری استفاده کنید تا هم امنیت کاربران و هم مسئولیت حقوقی خودتان کمتر شود.

---

## فاز ۱ — اسکلت ریپو و زیرساخت پایه

**هدف:** ساختار پروژه، Docker Compose برای تست لوکال، CI پایه.

تسک‌ها:
- ساختار مونو-ریپو: `/dns-server`, `/proxy-server`, `/backend-api`, `/panel-frontend`, `/telegram-bot`, `/windows-client`, `/infra`
- `docker-compose.yml` برای اجرای لوکال CoreDNS + HAProxy + Redis + SQL Server (برای تست، قبل از استقرار روی سرور واقعی)
- فایل `.env.example` برای متغیرهای محیطی

**پرامپت پیشنهادی برای Claude Code:**
```
یک مونو-ریپو با ساختار زیر بساز:
- dns-server/ (پیکربندی CoreDNS + پلاگین کاستوم Go)
- proxy-server/ (پیکربندی HAProxy)
- backend-api/ (پروژه ASP.NET Core Web API با ساختار Clean Architecture)
- panel-frontend/ (یک PWA با React + Vite)
- telegram-bot/ (پروژه .NET یا Node.js برای بات)
- infra/ (اسکریپت‌های nftables, systemd unit files, docker-compose)
یک docker-compose.yml برای اجرای لوکال CoreDNS + Redis + SQL Server بساز تا بتوانم قبل از دیپلوی روی سرور واقعی تست کنم.
```

---

## فاز ۲ — لایه DNS (ایران)

**هدف:** CoreDNS که دامنه‌های عادی را به DNS عمومی ارجاع می‌دهد و دامنه‌های تحریمی/بازی را به سرور پراکسی خارج می‌فرستد.

تسک‌ها:
- نوشتن `Corefile` با پلاگین `forward` برای دامنه‌های عادی
- پلاگین کاستوم (یا استفاده از `hosts` + `rewrite`) برای دامنه‌های خاص بازی‌ها که باید resolve به IP سرور پراکسی خارج شوند
- اتصال CoreDNS به Redis برای خواندن لیست دامنه‌های "تحریمی" به صورت داینامیک (بدون نیاز به ریستارت سرویس)
- health-check و متریک (Prometheus exporter داخلی CoreDNS)

**پرامپت پیشنهادی:**
```
یک پیکربندی CoreDNS بساز که:
۱. دامنه‌های داخل یک لیست در Redis (key: blocked_domains) را به IP مشخصی از سرور پراکسی خارج ریدایرکت کند
۲. بقیه دامنه‌ها را به 1.1.1.1 و 8.8.8.8 فوروارد کند
۳. متریک Prometheus را روی پورت 9153 expose کند
۴. یک اسکریپت Go یا پلاگین ساده بنویس که هر ۳۰ ثانیه لیست دامنه‌ها را از Redis reload کند بدون ریستارت سرویس
```

---

## فاز ۳ — تانل بین ایران و خارج

**هدف:** یک تانل WireGuard پایدار بین دو سرور.

تسک‌ها:
- اسکریپت نصب و کانفیگ خودکار WireGuard روی هر دو سرور
- systemd service برای اطمینان از بالا ماندن تانل (auto-restart)
- اسکریپت مانیتورینگ که وضعیت تانل را چک می‌کند و در صورت قطعی، هندشیک مجدد بزند یا آلارم بفرستد (تلگرام/ایمیل)

**پرامپت پیشنهادی:**
```
اسکریپت‌های Bash برای نصب و کانفیگ WireGuard بین دو سرور (ایران به عنوان کلاینت، خارج به عنوان سرور) بنویس.
یک systemd timer/service اضافه کن که هر ۶۰ ثانیه وضعیت تانل را با ping چک کند و اگر قطع بود، سرویس wg-quick را ریستارت کند و پیام به یک وبهوک تلگرام بفرستد.
```

---

## فاز ۴ — لایه Proxy (خارج از کشور)

**هدف:** HAProxy در حالت TCP passthrough (لایه ۴) برای عبور بدون دیکریپت.

تسک‌ها:
- کانفیگ HAProxy با `mode tcp` و SNI-based routing (پلاگین SNI inspection)
- Backend pool برای هر بازی/سرویس (Xbox, PSN, Epic, EA, Riot, Discord Voice)
- Health-check روی بک‌اندها

**پرامپت پیشنهادی:**
```
یک haproxy.cfg بساز که:
۱. روی پورت 443 با mode tcp و req.ssl_sni کار کند
۲. بر اساس SNI، ترافیک را به backend مربوط به هر سرویس (لیست دامنه‌ها را پارامتری کن) هدایت کند
۳. UDP relay ساده (با socat یا HAProxy UDP mode جدید) برای ترافیک صوتی Vivox/Discord اضافه کن
۴. لاگ‌ها را minimal نگه دار (فقط برای دیباگ، بدون ذخیره طولانی‌مدت)
```

---

## فاز ۵ — Firewall و مدیریت IP پویا

**هدف:** فقط IPهای دارای اشتراک فعال به پورت ۵۳ و سرویس‌های دیگر دسترسی داشته باشند.

تسک‌ها:
- اسکریپت nftables/ipset که یک "set" پویا از IPهای مجاز نگه می‌دارد
- API endpoint در بک‌اند که با فراخوانی، IP را به ipset اضافه/حذف کند (با TTL — مثلاً بعد از ۲۴ ساعت بدون رفرش خودکار حذف شود)
- Rate limiting برای جلوگیری از سوءاستفاده از API ثبت IP

**پرامپت پیشنهادی:**
```
اسکریپت nftables بساز که یک set به اسم allowed_ips نگه دارد و فقط IPهای داخل آن به پورت 53/tcp,udp دسترسی داشته باشند، بقیه drop شوند.
یک ابزار کمکی (CLI یا systemd service) بساز که هر IP در ipset را با TTL نگه دارد و بعد از انقضا خودکار حذفش کند (با استفاده از nftables timeout feature یا یک cron job که lastSeen را در Redis چک می‌کند).
```

---

## فاز ۶ — Backend API (هسته سیستم)

**هدف:** مدیریت کاربران، اشتراک‌ها، ثبت IP، و هماهنگی با فایروال.

Endpoints اصلی:
- `POST /api/auth/login`
- `POST /api/subscription/update-ip` (این IP کاربر را در Redis + nftables ثبت می‌کند)
- `GET /api/subscription/status`
- `POST /api/admin/domains` (مدیریت لیست دامنه‌های هدایت‌شونده)
- `POST /api/webhook/telegram`

**پرامپت پیشنهادی:**
```
یک پروژه ASP.NET Core Web API با Clean Architecture بساز شامل:
- Entity: User, Subscription, IpBinding, GameDomain
- Endpoint POST /api/subscription/update-ip که:
  ۱. IP درخواست‌دهنده را از هدر واقعی (X-Forwarded-For در نظر گرفته با احتیاط) بخواند
  ۲. اشتراک کاربر را چک کند (فعال بودن)
  ۳. IP قبلی را از Redis/nftables حذف و IP جدید را جایگزین کند
  ۴. Rate limit: هر کاربر حداکثر ۱ درخواست در ۱۰ ثانیه
- استفاده از Entity Framework Core + SQL Server برای دیتای دائمی، StackExchange.Redis برای session سریع
- یک BackgroundService که هر ۵ دقیقه اشتراک‌های منقضی‌شده را از فایروال حذف کند
```

---

## فاز ۷ — پنل کاربری (PWA) و ربات تلگرام

**هدف:** رابط کاربری برای ثبت IP بدون نیاز به دانش فنی.

تسک‌ها:
- صفحه لاگین، صفحه داشبورد با دکمه «بروزرسانی IP فعلی من»
- PWA manifest برای نصب روی موبایل
- ربات تلگرام با کامند `/update`, `/status`, `/renew`

**پرامپت پیشنهادی:**
```
یک PWA با React بساز که:
۱. صفحه لاگین ساده
۲. داشبورد با نمایش IP فعلی کاربر (گرفته‌شده از request خودش) و دکمه‌ی «ثبت این IP»
۳. نمایش وضعیت اشتراک و تاریخ انقضا
manifest.json برای PWA اضافه کن تا قابل نصب روی موبایل باشد.

سپس یک ربات تلگرام (.NET یا Node) بساز که کامند /update را می‌گیرد، IP فرستنده را (از طریق چک IP در پیام یا external service) تشخیص می‌دهد و به همان API بک‌اند وصل می‌شود.
```

---

## فاز ۸ — کلاینت ویندوز (اختیاری، برای فاز بعدی)

یک اپ Tray کوچک (C# .NET یا Tauri) که هر ۵ دقیقه IP را چک و در صورت تغییر، خودکار به‌روزرسانی کند. این را می‌توانید در MVP کنار بگذارید و بعد از اعتبارسنجی بازار اضافه کنید.

---

## فاز ۹ — پایش خودکار دامنه‌های جدید بازی‌ها

**هدف:** وقتی بازی‌ها ساب‌دامنه جدید اضافه می‌کنند، خودکار شناسایی شود.

تسک‌ها:
- اسکریپتی که به صورت دوره‌ای از منابع عمومی (Certificate Transparency logs مثل crt.sh) ساب‌دامنه‌های جدید مرتبط با دامنه‌های اصلی بازی‌ها را استخراج کند
- ثبت خودکار در دیتابیس دامنه‌ها با وضعیت "pending review" تا ادمین تایید کند

**پرامپت پیشنهادی:**
```
یک BackgroundService در ASP.NET Core بساز که هر ۶ ساعت crt.sh را برای دامنه‌های اصلی (لیست پارامتری) کوئری بزند،
ساب‌دامنه‌های جدید را استخراج کند و آن‌هایی که قبلاً در دیتابیس GameDomain نیستند را با وضعیت Pending ذخیره کند تا از پنل ادمین تایید شوند.
```

---

## فاز ۱۰ — تست و راه‌اندازی نهایی روی سرور واقعی

1. دیپلوی CoreDNS و nftables روی سرور ایران
2. دیپلوی HAProxy روی سرور خارج
3. برقراری تانل WireGuard
4. دیپلوی بک‌اند + دیتابیس + Redis (می‌تواند روی سرور سوم یا همان سرور خارج باشد)
5. تست کامل مسیر: تغییر DNS روی یک سیستم → لاگین به یک بازی تست (مثلاً Epic Games) → بررسی لاگ HAProxy

---

## ترتیب پیشنهادی اجرا با Claude Code

بهتر است این فازها را **یکی‌یکی و جدا** به Claude Code بدهید (نه همه با هم)، چون هرکدام یک بخش مستقل از سیستم است:

1. فاز ۱ (اسکلت ریپو) → تست لوکال با Docker
2. فاز ۶ (Backend API) → چون هسته سیستم است و بقیه به آن وابسته‌اند
3. فاز ۲ و ۵ (DNS + Firewall) → با هم چون به هم وابسته‌اند
4. فاز ۴ (Proxy) 
5. فاز ۳ (تانل) → بعد از این که دو سرور واقعی دارید
6. فاز ۷ (پنل + بات)
7. فاز ۹ (مانیتورینگ دامنه)
8. فاز ۸ (کلاینت ویندوز) — اختیاری

برای هر فاز، پرامپت پیشنهادی بالا را کپی کنید و به Claude Code بدهید؛ Claude Code می‌تواند مستقیم فایل‌ها را بسازد، تست بزند و کامیت کند.
